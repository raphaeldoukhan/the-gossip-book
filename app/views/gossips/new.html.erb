<header>
  <% if @gossip.errors.any? %>
    <div class="alert alert-warning mx-2 my-2 " role="alert">
    <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    <ul>
      <% @gossip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
    </div>
  <% end %>
</header>

<body class="bg-light">
    <div class="container">
  <div class="py-5 text-center">
    <img class="d-block mx-auto mb-4" src="/docs/4.3/assets/brand/bootstrap-solid.svg" alt="" width="72" height="72">
    <h2>Gossip form</h2>
    <p class="lead">Below is an example form built entirely with Bootstrap’s form controls. Each required form group has a validation state that can be triggered by attempting to submit the form without completing it.</p>
  </div>

  <div class="row">
    <div class="col-md-12 order-md-1">
      <%=form_for @gossip do |f|%>
        <div class="row form-group">
          <div class="col-md-3 mb-3">
            <label>Titre</label>
            <%= f.text_field :title, class: "form-control"%>
            <small class="text-muted">Entre 3 et 14 caractères</small>
            <div class="invalid-feedback">
              Titre trop long
            </div>
          <span style="opacity: 1; left: 270px; top: 44.5px; width: 19px; min-width: 19px; height: 13px; position: absolute; background-repeat: no-repeat; background-position: 0px 0px; border: none; display: inline; visibility: visible; z-index: auto;"></span></div>
          <div class="col-md-6 mb-3">
            <label>Contenue</label>
            <%= f.text_area :content, class: "form-control"%>
            <small class="text-muted">Vérifie tes sources !</small>
          <span style="opacity: 1; left: 270px; top: 44.5px; width: 19px; min-width: 19px; height: 13px; position: absolute; background-repeat: no-repeat; background-position: 0px 0px; border: none; display: inline; visibility: visible; z-index: auto;"></span></div>
          <div class="col-md-3 mb-3">
              <label>Tags</label>
              <%= f.select :tag_ids, options_for_select(@tags), :multiple => true, class: "form-control"  %>
          </div>
        </div>
        <%=f.submit class:"btn btn-primary btn-lg btn-block", value:"Valider"%>
      <%end%>
    </div>
  </div>
</body>