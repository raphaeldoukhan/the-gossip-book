<!DOCTYPE html>
<html>
  <head>
    <title>The Gossip Project</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">      
        <a class="navbar-brand" href="/">The Gossip Project</a>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item">
              <%=link_to "Gossips", gossips_path, :class => "nav-link"%>
            </li>
            <li class="nav-item">
              <%=link_to "Users", users_path, :class => "nav-link"%>
            </li>
            <li class="nav-item">
              <%=link_to "Cities", cities_path, :class => "nav-link"%>
            </li>
            <li class="nav-item">
              <%=link_to "Team", team_path, :class => "nav-link"%>
            </li>
            <li class="nav-item">
            <%=link_to "Contact", contact_path, :class => "nav-link"%>
            </li>
          </ul>
        </div>
      </div>
      <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        <ul class="navbar-nav ml-auto">
        <%if !logged_in?%>
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Connexion</a>
            <div class="dropdown-menu" aria-labelledby="dropdown01">
              <%=link_to "S'inscrire", new_user_path, :class => "nav-link text-dark"%>
              <%=link_to "Se connecter", new_session_path, :class => "nav-link text-dark"%>
            </div>
          </li>
          <%end%>
          <%if logged_in?%>
          <li class="nav-item">          
            <%= link_to "Se déconnecter", session_path(session.id), method: :delete, :class => "nav-link" %>
          </li>
          <%end%>
        </ul>
    </div>
    </nav>
    
    <div class="text-center" style="position: absolute; left: 50%;">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %> mx-5 mt-2 px-5" style="position: relative; left: -50%;" ><%= value %></div>
      <% end %>
    </div>
      <%= yield %>
      <%# <%= debug(params) if Rails.env.development?       %> <%#%%>
    </div>
  </body>
</html>